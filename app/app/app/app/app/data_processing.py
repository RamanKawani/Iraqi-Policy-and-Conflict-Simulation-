import requests

def get_policy_data(policy_id):
    # Placeholder function for fetching data from external sources (e.g., APIs)
    # For now, we simulate this by returning static data
    policy_data = {
        'id': policy_id,
        'name': 'Policy Example',
        'description': 'Detailed description of the policy.'
    }
    return policy_data
